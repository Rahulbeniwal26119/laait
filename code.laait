let arr = [1,2,3,4]
let map = function(arr, f){
let iter = function(arr, accumulated){
    if (len(arr) == 0) {
        accumulated
    } else {
        iter(rest(arr), push(accumulated, f(first(arr))))
    }
}
iter(arr, [])
};

